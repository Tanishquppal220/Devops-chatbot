# Tutorial: frontend

The **DevOps Copilot** is a specialized AI chat interface designed to streamline *infrastructure management* and *deployment* tasks. It features a suite of **specialized agents**—such as experts for Dockerfiles and Production Readiness—that provide real-time, *streamed responses* to user queries. The application balances a retro-modern UI with robust **conversation persistence**, ensuring that technical DevOps workflows are both interactive and easily retrievable.


**Source Repository:** [None](None)

```mermaid
flowchart TD
    A0["Chat State Orchestrator (useChat Hook)
"]
    A1["SSE Streaming Engine
"]
    A2["The App Shell (Layout System)
"]
    A3["Specialized Agent Routing
"]
    A4["Intelligent Markdown Renderer
"]
    A5["Interactive Welcome & Suggestion System
"]
    A6["Generation & Sync Status Indicators
"]
    A7["Conversation Persistence (CRUD)
"]
    A0 -- "Initiates data stream" --> A1
    A0 -- "Synchronizes history" --> A7
    A2 -- "Hosts state" --> A0
    A0 -- "Configures active mode" --> A3
    A0 -- "Feeds data for formatting" --> A4
    A5 -- "Injects initial prompts" --> A0
    A0 -- "Drives visual feedback" --> A6
```

## Chapters

1. [The App Shell (Layout System)
](01_the_app_shell__layout_system__.md)
2. [Chat State Orchestrator (useChat Hook)
](02_chat_state_orchestrator__usechat_hook__.md)
3. [Interactive Welcome & Suggestion System
](03_interactive_welcome___suggestion_system_.md)
4. [Specialized Agent Routing
](04_specialized_agent_routing_.md)
5. [Conversation Persistence (CRUD)
](05_conversation_persistence__crud__.md)
6. [SSE Streaming Engine
](06_sse_streaming_engine_.md)
7. [Intelligent Markdown Renderer
](07_intelligent_markdown_renderer_.md)
8. [Generation & Sync Status Indicators
](08_generation___sync_status_indicators_.md)


---

Generated by [AI Codebase Knowledge Builder](https://github.com/The-Pocket/Tutorial-Codebase-Knowledge)

```mermaid
flowchart TD
  U[User] -->|Sends query| UI[React Chat UI]

  UI -->|HTTP request| API[FastAPI Backend]

  API -->|Route request| R[Intent Router]
  R -->|Selects specialist| A[Specialist Agent]

  A -->|Optional| S[Scan local codebase]
  S --> C[Generate structured context]

  A -->|System prompt + context + user query| G[Gemini API]

  G -->|Streamed tokens| SSE[SSE Stream]
  SSE -->|Stream response| UI

  UI -->|Render streamed answer| U
```

```mermaid
flowchart TD
    %% ---------- Frontend ----------
    U["User"] --> UI["Chat UI (App Shell / Layout)"]
    UI --> UC["Chat State Orchestrator (useChat Hook)"]
    UC --> W["Welcome & Suggestions System\n(injects starter prompts)"]
    W --> UC

    UC --> SR["Selects Active Mode\n(Specialized Agent Routing - FE)"]
    SR --> REQ["HTTP Request: user message + session info\n+ selected mode"]

    %% ---------- Backend Entry ----------
    REQ --> API["Backend API Endpoint\n(chat/agent invoke)"]

    %% ---------- Agent Runtime / Orchestration ----------
    API --> MS["Markdown Prompt Library\n(system + domain constraints)"]
    API --> AS["AgentState (Shared Notepad)\ncontext + scratchpad + metadata"]
    MS --> IR["Intelligent Intent Routing (BE)\nselects agent/tool path"]
    AS --> IR

    IR --> RT["Hybrid Model Runtime\n(Cloud & Edge LLM calls)"]
    IR --> SG["StateGraph Orchestration\n(plans + executes steps)"]

    %% ---------- Code / Document Context (optional) ----------
    SG --> AST["AST-Based Code Analysis\n(builds code context)"]
    AST --> AS

    %% ---------- Memory / Persistence ----------
    SG --> MEM["Persistent Chat Store\n(conversation CRUD)"]
    MEM --> AS

    %% ---------- Streaming Response ----------
    SG --> SSEBE["Real-time SSE Streaming\n(emits tokens + events)"]
    SSEBE --> SSEFE["SSE Streaming Engine (Frontend)"]
    SSEFE --> UC
    UC --> REND["Intelligent Markdown Renderer\n(format response)"]
    REND --> UI
    UI --> U

    %% ---------- Status / UX Feedback ----------
    UC --> STAT["Generation & Sync Status Indicators"]
    STAT --> UI

    %% ---------- Persist messages during stream ----------
    SSEBE --> MEM
```
